{
  "validationLogger": {
    "category": "validation",
    "redactFields": [
      "metadata.user.password",
      "metadata.user.ssn",
      "metadata.secretKey"
    ],
    "required": {
      "info": ["code", "context"],
      "warn": ["code", "context"],
      "error": ["code", "context"]
    }
  },
  "authLogger": {
    "category": "authentication",
    "redactFields": [
      "metadata.user.password",
      "metadata.user.ssn",
      "metadata.secretKey"
    ],
    "required": {
      "info": ["code", "context"],
      "warn": ["code", "context"],
      "error": ["code", "context"]
    }
  },
  "systemLogger": {
    "category": "system",
    "redactFields": ["config.apiKey", "config.authToken"],
    "required": {
      "info": ["code", "context"],
      "warn": ["code", "context"],
      "error": ["code", "context"]
    }
  },
  "usageLogger": {
    "category": "usage",
    "redactFields": [
      "metadata.user.password",
      "metadata.user.ssn",
      "metadata.secretKey"
    ],
    "required": {
      "info": ["code", "context"],
      "warn": ["code", "context"],
      "error": ["code", "context"]
    }
  }
}
